trigger:
- main

pool:
  name: 'agent'
  demands:
    - agent.name -equals hostagent-vm

variables:
    buildConfiguration: 'Release'

steps:
- script: dotnet buile --configuration $(buildConfiguration)
  displayName: 'dotnet build $(buildConfiguration)'

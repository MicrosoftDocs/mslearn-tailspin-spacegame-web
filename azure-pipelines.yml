trigger:
- '*'

pool: dell

variables:
  buildConfiguration: 'Release'
  wwwrootDir: 'Tailspin.SpaceGame.Web/wwwroot'
  dotnetSdkVersion: '5.x'
  serviceName: 'DefaultService'

steps:
# - task: UseDotNet@2
#   displayName: 'Use .NET SDK $(dotnetSdkVersion)'
#   inputs:
#     packageType: sdk
#     version: '$(dotnetSdkVersion)'

# - task: Npm@1
#   displayName: 'Run npm install'
#   inputs:
#     verbose: false

# - script: './node_modules/.bin/node-sass $(wwwrootDir) --output $(wwwrootDir)'
#   displayName: 'Compile Sass assets'

# - task: gulp@1
#   displayName: 'Run gulp tasks'

# - script: 'echo "$(Build.DefinitionName), $(Build.BuildId), $(Build.BuildNumber)" > buildinfo.txt'
#   displayName: 'Write build info'
#   workingDirectory: $(wwwrootDir)

# - task: DotNetCoreCLI@2
#   displayName: 'Restore project dependencies'
#   inputs:
#     command: 'restore'
#     projects: '**/*.csproj'

# - template: templates/build.yml
#   parameters:
#     buildConfiguration: 'Debug'

# - template: templates/build.yml
#   parameters:
#     buildConfiguration: 'Release'

# - task: PublishBuildArtifacts@1
#   displayName: 'Publish Artifact: drop'
#   condition: succeeded()

- script: 'curl -X PUT -H"Authorization: Bearer $(System.AccessToken)" -H "Content-Length: 0" "$(System.CollectionUri)Space Game%20-%20web%20-%20Pipeline/_apis/build/builds/$(Build.BuildId)/tags/service=$(serviceName)?api-version=7.1-preview.3"'
  displayName: Tag Build

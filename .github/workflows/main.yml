on:
  label:
    types:
      - created
  issues:  
    types: [opened, labeled]       
  push:    
    branches: main
      - main
jobs:
  firstjob:
    runs-on: ubuntu-latest
    steps:    
     - run: echo "hello world"      